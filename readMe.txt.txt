Steps to start the project:
  1.Start the Mongodb server.
  2.Then type npm install from command line.
  3.Then type node index.js
  4.Ready to go to the http://localhost:3000

Note:You need to have some files in your database to see the results 